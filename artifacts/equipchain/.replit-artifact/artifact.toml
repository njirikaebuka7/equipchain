kind = "web"
previewPath = "/"
title = "EquipChain Global Ltd"
version = "1.0.0"
id = "artifacts/equipchain"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21250

[services.development]
run = "pnpm --filter @workspace/equipchain run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/equipchain", "run", "build" ]
publicDir = "artifacts/equipchain/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21250"
BASE_PATH = "/"
